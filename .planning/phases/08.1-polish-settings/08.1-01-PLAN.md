---
phase: 08.1-polish-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - css/shared.css
  - css/cards.css
  - index.html
autonomous: true
requirements:
  - POL-01
  - POL-02
  - POL-03

must_haves:
  truths:
    - "Card games (Solitaire, Spider, FreeCell) display on a green felt background"
    - "Woord Soek displays on a parchment/cream background with readable dark text"
    - "Face-down cards show the classic MS blue crosshatch pattern"
    - "Sudoku retains warm-dark theme unchanged"
  artifacts:
    - path: "css/shared.css"
      provides: "Green felt theme class, parchment theme class with full colour overrides"
      contains: "theme-felt"
    - path: "css/cards.css"
      provides: "MS blue crosshatch card back pattern as default"
      contains: "card-back-bg"
    - path: "index.html"
      provides: "Theme classes applied to game containers"
      contains: "theme-felt"
  key_links:
    - from: "css/shared.css"
      to: "index.html"
      via: "CSS class on game containers"
      pattern: "theme-felt|theme-parchment"
    - from: "css/cards.css"
      to: "all card games"
      via: "face-down card back default colours"
      pattern: "card-back-bg.*#1a3d7a"
---

<objective>
Apply MS Solitaire-style green felt backgrounds to all three card games, parchment background to Woord Soek, and replace the default card back pattern with the classic MS blue crosshatch.

Purpose: Dad will instantly recognise the classic MS Solitaire look. Woord Soek gets a distinct old word puzzle book feel.
Output: Updated CSS themes and game container classes in index.html.
</objective>

<execution_context>
@/home/trashdev/.claude/get-shit-done/workflows/execute-plan.md
@/home/trashdev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-polish-settings/08.1-CONTEXT.md
@.planning/phases/08.1-polish-settings/08.1-RESEARCH.md

@css/shared.css
@css/cards.css
@index.html

<interfaces>
<!-- Existing CSS variables in cards.css that will be modified: -->
From css/cards.css:
```css
:root {
  --card-back-bg: #2c5f8a;
  --card-back-pattern: #1e4568;
  --card-back-border: #4a7fa8;
}
.card.face-down {
  background: var(--card-back-bg);
  border: 2px solid var(--card-back-border);
  background-image: repeating-linear-gradient(45deg, ...);
}
```

<!-- Game containers in index.html: -->
- `.sol-container` (Solitaire)
- `.spd-container` (Spider)
- `.fc-container` (FreeCell)
- `#screen-woordsoek` (Woord Soek)
- `.sdk-container` (Sudoku -- keep warm-dark)

<!-- Existing Woord Soek cell CSS classes that need parchment overrides: -->
- `.ws-cell` (background: var(--bg-card))
- `.ws-cell.selected` (background: var(--accent-gold))
- `.ws-cell.preview` (background: rgba(212,162,58,0.25))
- `.ws-cell.hint-flash` (blue flash animation)
- `.ws-word-item` (background: var(--bg-card))
- `.ws-word-item.found` (text-decoration: line-through)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add green felt and parchment theme CSS + update card backs</name>
  <files>css/shared.css, css/cards.css</files>
  <action>
1. In `css/cards.css`, replace the default card back variables to MS blue crosshatch:
   - `--card-back-bg: #1a3d7a` (deep blue)
   - `--card-back-pattern: #0f2a56` (darker blue for pattern)
   - `--card-back-border: #3a6db0` (lighter blue border)
   - Replace the `repeating-linear-gradient(45deg, ...)` on `.card.face-down` with a crosshatch grid pattern using TWO repeating-linear-gradients (0deg and 90deg) at 6px/7px intervals per RESEARCH.md example.
   - Add an `::after` inner border (already exists but update the border colour to `rgba(255,255,255,0.2)` for the MS look).

2. In `css/shared.css`, at the end (after FreeCell section), add a new section `/* ══════════════ GAME THEMES ══════════════ */`:

   a. `.theme-felt` class:
      - `background-color: #1a7e3c` (MS green)
      - `background-image` with two radial gradients for subtle light variation (per RESEARCH.md)
      - Override `--bg-surface` to a slightly darker green for headers
      - Override `--border-subtle` to a green-tinted border

   b. `.theme-parchment` class:
      - `background-color: #f0e6cc`
      - `background-image` with subtle radial gradient warmth
      - Override ALL colour variables for light theme: `--bg-base`, `--bg-surface`, `--bg-card`, `--text-primary`, `--text-secondary`, `--text-muted`, `--border-subtle`, `--border-accent`
      - Specific overrides for Woord Soek cells on parchment:
        - `.theme-parchment .ws-cell` — background: #e6d9b8, colour: #3a3020
        - `.theme-parchment .ws-cell.selected` — background: #c8973a, colour: #fff
        - `.theme-parchment .ws-cell.preview` — background: rgba(200,151,58,0.35)
        - `.theme-parchment .ws-word-item` — background: #dfd2b0, colour: #3a3020
        - `.theme-parchment .ws-word-item.found` — colour: #8a7d65
        - `.theme-parchment .game-header` — background: #e2d5b5, border-colour adjusted
        - `.theme-parchment .btn-back` — colour and border adjusted for light bg
        - `.theme-parchment .btn-icon` — background/colour adjusted for light bg
        - `.theme-parchment .badge` — adjusted for light bg
        - `.theme-parchment .timer` — colour adjusted for light bg
        - `.theme-parchment .surface` — background adjusted
        - `.theme-parchment h3` — colour adjusted
      - Test ALL interactive states: default, selected, preview, found, hint-flash to ensure contrast on light background (per Pitfall 4 in RESEARCH.md)

   c. `.theme-felt .game-header` overrides:
      - Darker green background for the header bar to contrast with the felt
      - Gold accent colours remain (they contrast well with green)

Do NOT change any existing warm-dark `:root` variables. The themes work via CSS class overrides only. Do NOT convert any card dimensions from px to rem. Do NOT add a theme toggle.
  </action>
  <verify>
    <automated>cd /home/trashdev/projects/congames && grep -c "theme-felt" css/shared.css && grep -c "theme-parchment" css/shared.css && grep "card-back-bg" css/cards.css | head -1</automated>
  </verify>
  <done>Green felt theme class defined with full colour overrides. Parchment theme class defined with all Woord Soek element overrides for contrast. Card back pattern changed to MS blue crosshatch. No existing warm-dark variables modified.</done>
</task>

<task type="auto">
  <name>Task 2: Apply theme classes to game containers in index.html</name>
  <files>index.html</files>
  <action>
1. Find the Woord Soek screen section (`#screen-woordsoek`). Add class `theme-parchment` to the outermost div inside it (the one with `style="display:flex;flex-direction:column;height:100%"`). This wraps the entire Woord Soek game including header.

2. Find the Solitaire container (`div.sol-container`). Add class `theme-felt` to it.

3. Find the Spider container (`div.spd-container`). Add class `theme-felt` to it.

4. Find the FreeCell container (`div.fc-container`). Add class `theme-felt` to it.

5. Do NOT add any theme class to the Sudoku container -- it keeps warm-dark per locked decision.

6. Do NOT add any theme class to the welcome screen -- it keeps warm-dark.

7. The Woord Soek difficulty button currently uses gear icon (&#9881;). Change it to a bars/slider icon to avoid conflict with the settings gear that Plan 03 will add. Use &#9776; (hamburger/bars icon) or a text label. Per RESEARCH.md Pitfall 6. Change the icon content to `&#9776;` and update the title to "Moeilikheid".
  </action>
  <verify>
    <automated>cd /home/trashdev/projects/congames && grep -c "theme-felt" index.html && grep -c "theme-parchment" index.html</automated>
  </verify>
  <done>Three card game containers have `theme-felt` class. Woord Soek container has `theme-parchment` class. Sudoku unchanged. Woord Soek difficulty button icon changed to avoid gear conflict.</done>
</task>

</tasks>

<verification>
- Open each game screen and verify background colour matches theme:
  - Solitaire/Spider/FreeCell: green felt background
  - Woord Soek: parchment/cream background with readable dark text
  - Sudoku: warm-dark unchanged
- Face-down cards show blue crosshatch pattern in all card games
- All Woord Soek interactive states (selected, preview, found, hint-flash) remain visible on parchment
- No card layout dimensions affected by theme changes
</verification>

<success_criteria>
Card games display classic MS green felt look. Woord Soek has warm parchment feel. Card backs show blue crosshatch. Sudoku unchanged. All text remains readable with proper contrast.
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-polish-settings/08.1-01-SUMMARY.md`
</output>
