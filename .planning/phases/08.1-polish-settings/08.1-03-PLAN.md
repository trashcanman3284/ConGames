---
phase: 08.1-polish-settings
plan: 03
type: execute
wave: 2
depends_on:
  - 08.1-01
files_modified:
  - index.html
  - games/sudoku/ui.js
  - games/woordsoek/ui.js
  - games/solitaire/ui.js
  - games/spider/ui.js
  - games/freecell/ui.js
autonomous: true
requirements:
  - POL-05
  - POL-06

must_haves:
  truths:
    - "All 5 games have a gear icon button that opens a standardized settings modal"
    - "Sudoku settings modal has toggles for timer, highlight same numbers, show errors"
    - "Woord Soek settings modal has toggles for timer, auto-continue, layout position"
    - "Font size setting (Klein/Medium/Groot) on welcome screen propagates to game text without breaking card/grid layouts"
  artifacts:
    - path: "index.html"
      provides: "Sudoku settings modal DOM, Woord Soek settings modal DOM, Sudoku gear button"
      contains: "sdk-settings-modal"
    - path: "games/sudoku/ui.js"
      provides: "Settings modal open/close + toggle wiring for Sudoku"
      contains: "sdk-settings-modal"
    - path: "games/woordsoek/ui.js"
      provides: "Settings modal open/close + toggle wiring for Woord Soek"
      contains: "ws-settings-modal"
  key_links:
    - from: "index.html #sdk-settings-btn"
      to: "games/sudoku/ui.js"
      via: "click handler opens sdk-settings-modal"
      pattern: "sdk-settings-modal"
    - from: "index.html #ws-settings-btn"
      to: "games/woordsoek/ui.js"
      via: "click handler opens ws-settings-modal"
      pattern: "ws-settings-modal"
    - from: "Settings.applyFontSize()"
      to: "all game text"
      via: "root font-size change, text uses rem"
      pattern: "applyFontSize"
---

<objective>
Standardize settings modals across all 5 games (adding new ones for Sudoku and Woord Soek) and audit font size propagation to ensure text scales properly while card/grid layouts stay fixed.

Purpose: Consistent settings experience across all games. Font size actually works end-to-end for Dad's comfort.
Output: All 5 games have gear-icon settings modals with identical styling. Font size properly scales text only.
</objective>

<execution_context>
@/home/trashdev/.claude/get-shit-done/workflows/execute-plan.md
@/home/trashdev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08.1-polish-settings/08.1-CONTEXT.md
@.planning/phases/08.1-polish-settings/08.1-RESEARCH.md
@.planning/phases/08.1-polish-settings/08.1-01-SUMMARY.md

@index.html
@games/sudoku/ui.js
@games/woordsoek/ui.js
@games/solitaire/ui.js
@games/spider/ui.js
@games/freecell/ui.js
@js/settings.js

<interfaces>
<!-- Existing settings modal pattern (from Solitaire): -->
From index.html:
```html
<div class="win-overlay" id="sol-settings-modal">
  <div class="win-card">
    <h2 style="font-size:1.4rem;">Spel Instellings</h2>
    <div class="setting-row">
      <label>Toggle Label</label>
      <label class="toggle"><input type="checkbox" id="sol-toggle-NAME" /><span class="toggle-track"></span></label>
    </div>
    <button class="btn btn-secondary" style="margin-top:16px;width:100%;" id="sol-settings-close">Sluit</button>
  </div>
</div>
```

<!-- Existing settings wiring pattern (from Solitaire ui.js): -->
- Settings button: `id="sol-settings-btn"` with gear icon
- Open: `showOverlay('sol-settings-modal')`
- Close: `hideOverlay('sol-settings-modal')`
- Toggle sync on open: `document.getElementById('sol-toggle-hints').checked = Settings.get('sol-showHints', true)`
- Toggle change: `onchange` calls `Settings.set(key, this.checked)` and applies

<!-- Settings.js API: -->
```javascript
Settings.get(key, defaultValue)
Settings.set(key, value)
Settings.applyFontSize()  // sets root font-size to 16/18/21px based on stored 'fontSize'
```

<!-- Current Sudoku header (no gear button yet): -->
```html
<div style="display:flex;gap:8px;">
  <button class="btn-icon" id="sdk-undo-btn" title="Ontdoen">&#8617;</button>
  <button class="btn-icon" id="sdk-pause-btn" title="Pouse">&#9646;&#9646;</button>
  <button class="btn-icon" id="sdk-newgame-btn" title="Nuwe Spel">&#x1F504;</button>
</div>
```

<!-- Current Woord Soek header buttons: -->
```html
<div style="display:flex;gap:8px;">
  <button class="btn-icon" id="ws-hint-btn" onclick="WoordSoekUI.hint()" title="Wenk">&#128161;</button>
  <button class="btn-icon" id="ws-difficulty-btn" onclick="WoordSoekUI.showDifficultyModal()" title="Moeilikheid">&#9881;</button>
  <button class="btn-icon" id="ws-layout-btn" onclick="WoordSoekUI.toggleLayout()" title="Skuif woordlys">&#8645;</button>
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Sudoku and Woord Soek settings modals + gear buttons in index.html</name>
  <files>index.html</files>
  <action>
1. **Sudoku gear button**: In the Sudoku header, add a gear button after the new-game button:
   ```html
   <button class="btn-icon" id="sdk-settings-btn" title="Instellings">&#9881;</button>
   ```

2. **Sudoku settings modal**: Add after the existing `sdk-loading-overlay` div, before the win animation div. Follow the exact same pattern as Solitaire/Spider/FreeCell modals:
   ```html
   <div class="win-overlay" id="sdk-settings-modal">
     <div class="win-card">
       <h2 style="font-size:1.4rem;">Spel Instellings</h2>
       <div class="setting-row">
         <label>Wys tyd</label>
         <label class="toggle"><input type="checkbox" id="sdk-toggle-timer" /><span class="toggle-track"></span></label>
       </div>
       <div class="setting-row">
         <label>Merk dieselfde nommers</label>
         <label class="toggle"><input type="checkbox" id="sdk-toggle-highlight" /><span class="toggle-track"></span></label>
       </div>
       <div class="setting-row">
         <label>Wys foute</label>
         <label class="toggle"><input type="checkbox" id="sdk-toggle-errors" /><span class="toggle-track"></span></label>
       </div>
       <button class="btn btn-secondary" style="margin-top:16px;width:100%;" id="sdk-settings-close">Sluit</button>
     </div>
   </div>
   ```

3. **Woord Soek gear button**: Add a settings gear button in the Woord Soek header button group (after the layout button):
   ```html
   <button class="btn-icon" id="ws-settings-btn" title="Instellings">&#9881;</button>
   ```

4. **Woord Soek settings modal**: Add after the `ws-confirm-modal` div, before the closing `</section>`. Same pattern:
   ```html
   <div class="win-overlay" id="ws-settings-modal">
     <div class="win-card">
       <h2 style="font-size:1.4rem;">Spel Instellings</h2>
       <div class="setting-row">
         <label>Wys tyd</label>
         <label class="toggle"><input type="checkbox" id="ws-toggle-timer" /><span class="toggle-track"></span></label>
       </div>
       <div class="setting-row">
         <label>Outo-hervat</label>
         <label class="toggle"><input type="checkbox" id="ws-toggle-autocontinue" /><span class="toggle-track"></span></label>
       </div>
       <div class="setting-row">
         <label>Woordlys onder</label>
         <label class="toggle"><input type="checkbox" id="ws-toggle-layout" /><span class="toggle-track"></span></label>
       </div>
       <button class="btn btn-secondary" style="margin-top:16px;width:100%;" id="ws-settings-close">Sluit</button>
     </div>
   </div>
   ```

5. **Remove Woord Soek layout toggle button** from header since it's now in settings modal. Remove the `ws-layout-btn` button element. Keep hint and difficulty buttons.

6. **Font size audit** in index.html styles: Check all `font-size` declarations in the inline `<style>` block.
   - Game text that should scale: `.game-name`, `.game-sub`, `.welcome-header p`, button labels -- ensure these use `rem` not `px`
   - Items that must NOT scale: `.game-btn` min-height (keep px), `.game-grid` gap (keep px), `.game-icon` size (keep rem -- it's decorative, slight scaling OK)
   - The `.ws-cell` font-size uses `clamp(0.85rem, 2.5vw, 1.3rem)` -- the rem base means it will scale slightly with font size. This is acceptable (grid cells use aspect-ratio so they won't overflow).
   - The `.ws-word-item` font-size is `0.95rem` -- will scale properly.
   - Sudoku `.sdk-cell` uses `clamp(1.2rem, 3vw, 2rem)` -- slight scaling OK, grid uses aspect-ratio.
   - Sudoku `.sdk-num-btn` uses `1.5rem` -- will scale, which is desired.
   - Sudoku `.sdk-note-digit` uses `clamp(0.45rem, 0.8vw, 0.7rem)` -- will scale slightly, desired per CONTEXT.md.
   - No changes needed to existing font sizes -- they already use rem and will scale with `applyFontSize()`.
  </action>
  <verify>
    <automated>cd /home/trashdev/projects/congames && grep -c "sdk-settings-modal" index.html && grep -c "ws-settings-modal" index.html && grep -c "sdk-settings-btn" index.html && grep -c "ws-settings-btn" index.html</automated>
  </verify>
  <done>Sudoku and Woord Soek each have a gear button in header and a settings modal following the standard pattern. Woord Soek layout toggle moved from header to settings modal. Font size audit confirms rem-based text sizing works correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Wire settings modal JS in Sudoku and Woord Soek + minor alignment in others</name>
  <files>games/sudoku/ui.js, games/woordsoek/ui.js, games/solitaire/ui.js, games/spider/ui.js, games/freecell/ui.js</files>
  <action>
**Sudoku (games/sudoku/ui.js):**
1. In the `init()` function, add event listeners:
   - `document.getElementById('sdk-settings-btn').addEventListener('click', openSettings)` (or use onclick in HTML)
   - `document.getElementById('sdk-settings-close').addEventListener('click', closeSettings)`
   - Wire toggle change handlers:
     - `sdk-toggle-timer`: `Settings.set('sdk-showTimer', this.checked)`, toggle timer display: `document.getElementById('sdk-timer').style.display = this.checked ? '' : 'none'`
     - `sdk-toggle-highlight`: `Settings.set('sdk-highlightSame', this.checked)` -- if the Sudoku UI already highlights same numbers, wire this to toggle that behaviour. Read the current code to find how highlighting works and add the toggle check.
     - `sdk-toggle-errors`: `Settings.set('sdk-showErrors', this.checked)` -- wire to control whether error checking runs automatically or only on explicit check button press
2. Create `openSettings()` function:
   - Sync toggle states from Settings: `document.getElementById('sdk-toggle-timer').checked = Settings.get('sdk-showTimer', true)` (repeat for each toggle)
   - Show modal: add 'active' class to `sdk-settings-modal`
3. Create `closeSettings()` function:
   - Remove 'active' class from `sdk-settings-modal`
4. Expose `openSettings` if needed by onclick in HTML, or wire via addEventListener in init.

**Woord Soek (games/woordsoek/ui.js):**
1. In `init()`, add event listeners:
   - `document.getElementById('ws-settings-btn').addEventListener('click', openSettings)` (or onclick)
   - `document.getElementById('ws-settings-close').addEventListener('click', closeSettings)`
   - Wire toggle change handlers:
     - `ws-toggle-timer`: `Settings.set('ws-showTimer', this.checked)`, toggle timer visibility
     - `ws-toggle-autocontinue`: `Settings.set('ws-auto-continue', this.checked)` -- already read by `onPuzzleComplete()`
     - `ws-toggle-layout`: `Settings.set('ws-layout-below', this.checked)`, call `toggleLayout()` equivalent to apply immediately
2. Create `openSettings()`:
   - Sync toggles: timer checked = `Settings.get('ws-showTimer', true)`, autocontinue = `Settings.get('ws-auto-continue', false)`, layout = `Settings.get('ws-layout-below', false)`
   - Show modal: add 'active' to `ws-settings-modal`
3. Create `closeSettings()`:
   - Remove 'active' from `ws-settings-modal`
4. Update `toggleLayout()` to also sync the toggle checkbox if it exists.
5. Remove the `onclick="WoordSoekUI.toggleLayout()"` reference since the button is removed from header (done in Task 1). Keep the function but call it from the toggle handler instead.
6. Expose `openSettings` and `closeSettings` in public API if using onclick in HTML.

**Solitaire, Spider, FreeCell (minor alignment):**
- Review each game's settings modal wiring. They already use the standard pattern.
- Only change needed: ensure the settings button handler uses the same function name pattern (`openSettings`/`closeSettings` or `showSettings`/`hideSettings`) for consistency. If they already work, no changes needed.
- Check that Solitaire and Spider settings modals have the same `style="margin-top:16px;width:100%;"` on the close button (they do from the HTML audit).
- No functional changes needed -- these three already have proper settings modals.

IMPORTANT:
- Do NOT remove any existing functionality from Sudoku or Woord Soek
- The Sudoku `sdk-toggle-highlight` should default to `true` (highlighting is currently always on)
- The Sudoku `sdk-toggle-errors` should default to `false` (errors only shown on explicit check button)
- Settings values persist via `Settings.get()/set()` -- they survive page refreshes
  </action>
  <verify>
    <automated>cd /home/trashdev/projects/congames && grep -c "sdk-settings" games/sudoku/ui.js && grep -c "ws-settings" games/woordsoek/ui.js</automated>
  </verify>
  <done>Sudoku has working settings modal with timer, highlight, and errors toggles. Woord Soek has working settings modal with timer, auto-continue, and layout toggles. All 5 games now have consistent gear-icon settings access.</done>
</task>

</tasks>

<verification>
- Open each game and verify gear icon opens a settings modal:
  - Solitaire: hints, timer, moves, scoring toggles (existing)
  - Spider: hints, timer, moves, scoring toggles (existing)
  - FreeCell: timer, moves toggles (existing)
  - Sudoku: timer, highlight, errors toggles (NEW)
  - Woord Soek: timer, auto-continue, layout toggles (NEW)
- All modals have identical styling: same width, same toggle style, same close button
- Toggle values persist after closing and reopening the modal
- Font size: change to "Groot" on welcome screen, verify game text (headers, buttons, word list, number pad) scales up while card dimensions and grid cells stay fixed
</verification>

<success_criteria>
All 5 games accessible via gear icon with standardized settings modal. Toggles persist via Settings. Font size Klein/Medium/Groot works end-to-end without breaking layouts.
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-polish-settings/08.1-03-SUMMARY.md`
</output>
