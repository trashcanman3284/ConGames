---
phase: 09-packaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sw.js
  - manifest.json
  - index.html
  - games/solitaire/index.html
  - games/spider/index.html
  - games/sudoku/index.html
  - games/freecell/index.html
autonomous: true
requirements:
  - PLT-01

must_haves:
  truths:
    - "Service worker CORE_ASSETS all use /ConGames/ prefix and sw.js does NOT cache itself"
    - "manifest.json start_url and scope use /ConGames/ prefix"
    - "SW registration in index.html uses /ConGames/sw.js with scope /ConGames/"
    - "Game redirect stubs redirect to /ConGames/ not /"
    - "Offline fallback returns /ConGames/index.html"
    - "Splash screen appears on cold start with fade-in icon and text, then fades out"
    - "Version number v1.0.0 appears as small footer text on welcome screen"
    - "Dateer op button in settings panel clears SW + caches and reloads"
  artifacts:
    - path: "sw.js"
      provides: "Service worker with /ConGames/ prefixed CORE_ASSETS and bumped cache version"
      contains: "/ConGames/"
    - path: "manifest.json"
      provides: "PWA manifest with correct scope and start_url"
      contains: "/ConGames/"
    - path: "index.html"
      provides: "Splash screen overlay, version footer, update button, fixed SW registration"
  key_links:
    - from: "index.html"
      to: "sw.js"
      via: "navigator.serviceWorker.register('/ConGames/sw.js')"
      pattern: "register.*ConGames.*sw\\.js"
    - from: "sw.js"
      to: "index.html"
      via: "offline fallback caches.match"
      pattern: "caches\\.match.*ConGames.*index\\.html"
---

<objective>
Fix all paths for GitHub Pages /ConGames/ subdirectory, finalize the service worker for robust offline caching, add branded splash screen, version footer, and manual update button.

Purpose: The app currently uses root-relative paths that break on GitHub Pages subdirectory hosting. This plan fixes all paths, polishes the SW, and adds the splash + update UX required for deployment.
Output: All project files with correct /ConGames/ paths, splash screen, version display, and "Dateer op" button.
</objective>

<execution_context>
@/home/trashdev/.claude/get-shit-done/workflows/execute-plan.md
@/home/trashdev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-packaging/09-CONTEXT.md
@.planning/phases/09-packaging/09-RESEARCH.md
@sw.js
@manifest.json
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all paths for /ConGames/ subdirectory and finalize service worker</name>
  <files>sw.js, manifest.json, index.html, games/solitaire/index.html, games/spider/index.html, games/sudoku/index.html, games/freecell/index.html</files>
  <action>
**sw.js changes:**
1. Bump CACHE_NAME from 'congames-v5' to 'congames-v6'
2. Prefix every entry in CORE_ASSETS with '/ConGames' (e.g., '/' becomes '/ConGames/', '/index.html' becomes '/ConGames/index.html', etc.)
3. Do NOT add sw.js itself to CORE_ASSETS (browser manages SW updates separately)
4. Add icon entries to CORE_ASSETS: '/ConGames/icons/icon-192.png' and '/ConGames/icons/icon-512.png'
5. Remove '/ConGames/games/woordsoek/index.html' from CORE_ASSETS — that file doesn't exist (Woord Soek is embedded in main index.html)
6. Update offline fallback: change `caches.match('/index.html')` to `caches.match('/ConGames/index.html')`

**manifest.json changes:**
1. Set `"start_url": "/ConGames/index.html"`
2. Add `"scope": "/ConGames/"`
3. Update icon src paths to absolute: `"/ConGames/icons/icon-192.png"` and `"/ConGames/icons/icon-512.png"`
4. Split `"purpose": "any maskable"` into separate icon entries — one with `"purpose": "any"`, one with `"purpose": "maskable"` for each size (4 entries total). This avoids PWABuilder warnings.

**index.html changes:**
1. Update SW registration from `register('/sw.js')` to `register('/ConGames/sw.js', { scope: '/ConGames/' })`

**Game redirect stubs (solitaire, spider, sudoku, freecell):**
1. Change `location.href = '/'` to `location.href = '/ConGames/'` (solitaire uses JS redirect)
2. Change `<meta http-equiv="refresh" content="0;url=/">` to `content="0;url=/ConGames/"` (spider, sudoku, freecell use meta refresh)
  </action>
  <verify>
    <automated>cd /home/trashdev/projects/congames && grep -c "ConGames" sw.js && grep "ConGames" manifest.json | head -5 && grep "ConGames/sw.js" index.html && grep "ConGames" games/solitaire/index.html games/spider/index.html games/sudoku/index.html games/freecell/index.html</automated>
  </verify>
  <done>All paths use /ConGames/ prefix. SW CORE_ASSETS has complete file list with prefix. Manifest has scope and corrected start_url. Game redirects point to /ConGames/. sw.js is NOT in CORE_ASSETS.</done>
</task>

<task type="auto">
  <name>Task 2: Add splash screen, version footer, and "Dateer op" update button</name>
  <files>index.html</files>
  <action>
**Splash screen — add at the very top of `<body>`, before any other content:**
Insert a full-viewport fixed overlay with inline CSS (no external dependency to avoid white flash):
- `id="splash"` with `position:fixed; inset:0; z-index:9999; background:#1a1610;` centered flex column
- Inside: a `#splash-icon` div with `opacity:0; transition:opacity 0.3s ease;` containing the four suit symbols in gold (#d4a23a): ♠ ♥ on first line, ♦ ♣ on second line, with "CSG" text below suits at smaller font-size
- Below icon: a `#splash-text` div with `opacity:0; transition:opacity 0.5s ease;` containing "Con se Speletjies" in Playfair Display (#d4a23a), font-size ~1.8rem
- Immediately after the splash div, add an inline `<script>` block:
  - Use requestAnimationFrame to set splash-icon opacity to 1
  - After 300ms setTimeout, set splash-text opacity to 1
  - On window 'load' event, wait 800ms minimum display time, then set splash opacity to 0 with 0.5s transition, then remove splash element after 500ms

**Version footer — add to the welcome screen section (#screen-welcome):**
- Add a small `<span>` at the bottom of the welcome screen with text "v1.0.0"
- Style inline: `position:absolute; bottom:8px; right:16px; font-size:0.7rem; color:rgba(240,230,208,0.3);`
- This is for remote debugging — Con won't notice it

**"Dateer op" button — add to the settings panel (#settings-panel):**
- Add a new section at the bottom of the settings panel, before the close area
- Label: "Dateer op" (Afrikaans for "Update")
- Style as a subtle button (not prominent — emergency use only)
- On click: call a `forceUpdate()` function that:
  1. Gets all SW registrations and unregisters each
  2. Gets all cache keys and deletes each cache
  3. After both complete, calls `location.reload()`
- Add the forceUpdate function in the existing `<script>` block at the bottom of index.html
  </action>
  <verify>
    <automated>cd /home/trashdev/projects/congames && grep -c "splash" index.html && grep "v1.0.0" index.html && grep "Dateer op" index.html && grep "forceUpdate" index.html</automated>
  </verify>
  <done>Splash screen fades in icon then text on cold start. Version v1.0.0 shown as tiny footer on welcome screen. "Dateer op" button in settings panel clears SW + caches and reloads page.</done>
</task>

</tasks>

<verification>
1. All files containing absolute paths use /ConGames/ prefix
2. sw.js CORE_ASSETS lists all cacheable files (minus sw.js itself, minus non-existent woordsoek/index.html)
3. manifest.json has scope, correct start_url, and split icon purpose entries
4. Game redirect stubs all point to /ConGames/
5. Splash screen div is first element in body with inline CSS
6. Version footer visible on welcome screen
7. "Dateer op" button present in settings panel
</verification>

<success_criteria>
- grep confirms /ConGames/ prefix in sw.js (20+ occurrences), manifest.json, index.html SW registration, all 4 game redirect stubs
- Splash div with id="splash" exists at top of body
- "v1.0.0" text exists in welcome screen
- "Dateer op" button and forceUpdate function exist in index.html
</success_criteria>

<output>
After completion, create `.planning/phases/09-packaging/09-01-SUMMARY.md`
</output>
