---
phase: 09-packaging
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - icons/icon-192.png
  - icons/icon-512.png
autonomous: false
requirements:
  - PLT-01
  - PLT-02

must_haves:
  truths:
    - "App icon PNG files exist at icons/icon-192.png and icons/icon-512.png"
    - "Icons show four card suits in gold/amber on dark brown background with CSG initials"
    - "All five games are playable end-to-end from the deployed GitHub Pages URL"
    - "App works fully offline after first load (service worker caches all assets)"
  artifacts:
    - path: "icons/icon-192.png"
      provides: "192x192 app icon for PWA manifest"
    - path: "icons/icon-512.png"
      provides: "512x512 app icon for PWA manifest"
  key_links:
    - from: "manifest.json"
      to: "icons/icon-192.png"
      via: "icons array src reference"
      pattern: "icon-192\\.png"
    - from: "manifest.json"
      to: "icons/icon-512.png"
      via: "icons array src reference"
      pattern: "icon-512\\.png"
---

<objective>
Generate app icon PNG files and verify the complete PWA is deployment-ready for GitHub Pages and APK packaging.

Purpose: The manifest references icon files that don't exist yet. Icons are needed for PWA install prompts and the APK. After icon generation, we verify the entire suite is ready for deployment.
Output: Two icon PNGs (192x192 and 512x512) and human verification that everything works.
</objective>

<execution_context>
@/home/trashdev/.claude/get-shit-done/workflows/execute-plan.md
@/home/trashdev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-packaging/09-CONTEXT.md
@.planning/phases/09-packaging/09-01-SUMMARY.md
@manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate app icon PNGs using canvas/SVG approach</name>
  <files>icons/icon-192.png, icons/icon-512.png</files>
  <action>
Create the `icons/` directory if it doesn't exist.

Generate two PNG icon files programmatically. Use one of these approaches (Claude's discretion on exact method):

**Option A — Node.js canvas (if available):**
Use a simple Node script to draw the icon on a canvas and save as PNG.

**Option B — HTML canvas + screenshot via Playwright:**
Create a temporary HTML file that renders the icon on a canvas element, then use Playwright to screenshot at 192x192 and 512x512.

**Option C — SVG conversion:**
Create an SVG with the icon design, then convert to PNG using available tools.

**Icon design spec (from user decisions):**
- Background: dark brown (#1a1610), rounded square shape (the PNG itself is square; Android handles rounding via adaptive icon masking)
- Four card suit symbols arranged in a 2x2 grid pattern: ♠ ♥ (top row), ♦ ♣ (bottom row)
- Suits colored in gold/amber (#d4a23a)
- "CSG" initials below the suits in gold/amber, smaller font
- The icon should have some padding from edges (safe zone for adaptive icon masking — keep content within center 66% of canvas)

Verify both files are valid PNGs with correct dimensions.
  </action>
  <verify>
    <automated>cd /home/trashdev/projects/congames && file icons/icon-192.png icons/icon-512.png 2>/dev/null && python3 -c "
from PIL import Image
i192 = Image.open('icons/icon-192.png')
i512 = Image.open('icons/icon-512.png')
assert i192.size == (192, 192), f'192 icon wrong size: {i192.size}'
assert i512.size == (512, 512), f'512 icon wrong size: {i512.size}'
print('Both icons correct dimensions')
" 2>/dev/null || echo "PIL not available, checking with file command" && ls -la icons/icon-192.png icons/icon-512.png</automated>
  </verify>
  <done>icons/icon-192.png (192x192) and icons/icon-512.png (512x512) exist as valid PNG files with the card suit + CSG design on dark brown background.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete PWA packaging</name>
  <files>icons/icon-192.png, icons/icon-512.png</files>
  <action>
What was built:
- All paths fixed for /ConGames/ GitHub Pages subdirectory
- Service worker finalized with complete CORE_ASSETS list
- Branded splash screen with fade-in animation
- Version v1.0.0 footer on welcome screen
- "Dateer op" manual update button in settings
- App icon PNGs generated (192x192 and 512x512)
- Manifest updated with scope, start_url, and icon references

Verification steps:

1. Icon check: Open icons/icon-192.png and icons/icon-512.png — verify they show four card suits + "CSG" in gold on dark brown

2. Local functionality check (start server if not running: python3 -m http.server 8080):
   - Open http://localhost:8080 in Chrome
   - Verify splash screen appears briefly with suits icon and "Con se Speletjies" text, then fades out
   - Verify "v1.0.0" appears as tiny text in bottom-right of welcome screen
   - Open settings panel — verify "Dateer op" button is present at bottom
   - Play each game briefly to confirm nothing broke from path changes

3. GitHub Pages deployment verification (after git push):
   - Visit https://trashcanman3284.github.io/ConGames/
   - Verify app loads correctly (splash -> welcome screen)
   - Open Chrome DevTools > Application > Service Workers — verify SW registered with /ConGames/ scope
   - Open DevTools > Application > Cache Storage — verify all assets cached
   - Toggle airplane mode / disable network — verify app still works offline
   - Check DevTools > Application > Manifest — verify manifest detected with icons

4. PWABuilder readiness (optional, for APK build):
   - Visit https://www.pwabuilder.com/
   - Enter https://trashcanman3284.github.io/ConGames/
   - Check that PWABuilder detects valid manifest, service worker, and icons

Type "approved" if icons look good and deployment works, or describe issues to fix.
  </action>
  <verify>
    <automated>echo "Human verification checkpoint — awaiting user approval"</automated>
  </verify>
  <done>Human confirms icons look correct, splash screen works, version footer visible, update button present, app loads on GitHub Pages, and offline mode works.</done>
</task>

</tasks>

<verification>
1. icons/icon-192.png and icons/icon-512.png exist with correct dimensions
2. Both icons are referenced in manifest.json
3. Complete end-to-end verification by human on deployed GitHub Pages URL
</verification>

<success_criteria>
- Two icon PNGs exist at correct sizes
- Human confirms splash screen, version footer, update button all work
- Human confirms app loads on GitHub Pages with offline support
- PWABuilder (optionally) confirms the PWA is packagable
</success_criteria>

<output>
After completion, create `.planning/phases/09-packaging/09-02-SUMMARY.md`
</output>
