---
phase: 12-ui-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - sw.js
  - manifest.json
autonomous: true
requirements:
  - UI-01
  - UI-03
  - UI-12
  - UI-13
must_haves:
  truths:
    - "Kruiswoordraaisel appears as a tappable game card on the welcome screen"
    - "A kruiswoord screen section exists with grid container, clue panels, hidden input, difficulty modal, win overlay, and quit confirmation"
    - "Service worker caches kruiswoord engine.js, ui.js, and clues.json"
    - "Version is bumped to 1.1.0 across sw.js, manifest.json, and index.html"
  artifacts:
    - path: "index.html"
      provides: "Kruiswoord game card, screen section HTML, script tags, stats refresh"
      contains: "screen-kruiswoord"
    - path: "sw.js"
      provides: "Kruiswoord files in CORE_ASSETS"
      contains: "kruiswoord/engine.js"
    - path: "manifest.json"
      provides: "Updated version"
      contains: "1.1.0"
  key_links:
    - from: "index.html game card"
      to: "Router.go('kruiswoord')"
      via: "onclick handler"
      pattern: "data-game=\"kruiswoord\""
    - from: "index.html script tags"
      to: "games/kruiswoord/engine.js and ui.js"
      via: "script src"
      pattern: "src=\"games/kruiswoord/"
    - from: "refreshStats()"
      to: "Settings.getStats('kruiswoord')"
      via: "games array"
      pattern: "'kruiswoord'"
---

<objective>
Add all HTML structure, welcome screen integration, and service worker cache entries for the kruiswoord game.

Purpose: Establish the complete DOM scaffold that KruiswoordUI.init() will wire to in Plan 02.
Output: index.html with kruiswoord screen section + game card, sw.js with cache entries, version bumped to 1.1.0.
</objective>

<execution_context>
@/home/trashdev/.claude/get-shit-done/workflows/execute-plan.md
@/home/trashdev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- KruiswoordEngine API (from games/kruiswoord/engine.js) -->
KruiswoordEngine.DIFFICULTY = {
  maklik:  { label: 'Maklik',  size: 9,  wordCount: 7  },
  medium:  { label: 'Medium',  size: 13, wordCount: 13 },
  moeilik: { label: 'Moeilik', size: 17, wordCount: 18 }
};
KruiswoordEngine.generate(difficulty, clues)  // returns {grid, words, difficulty, elapsedSeconds}
KruiswoordEngine.setLetter(row, col, letter)
KruiswoordEngine.checkWord(number, direction) // returns {correct, letters}
KruiswoordEngine.isComplete()                 // returns boolean
KruiswoordEngine.undo()                       // returns {success}
KruiswoordEngine.getState()                   // returns deep clone of state
KruiswoordEngine.getElapsed()                 // returns seconds since start

<!-- Existing modules -->
Router.go(screen), Router.back(), Router.onEnter(id, fn), Router.onLeave(id, fn)
Settings.recordWin('kruiswoord', secs), Settings.getStats('kruiswoord')
Audio.play('word_found'), Audio.play('board_finished')
window.showToast(msg), window.formatTime(seconds)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add kruiswoord screen section, game card, script tags, and stats to index.html</name>
  <files>index.html</files>
  <read_first>
    - index.html (full file — need welcome screen game-grid around line 461, screen-sudoku section around line 781 as template, script tags around line 1051, refreshStats around line 1075, version span around line 504)
    - games/kruiswoord/engine.js (lines 12-17 for DIFFICULTY constants, lines 651-660 for exported API)
    - .planning/phases/12-ui-integration/12-RESEARCH.md (patterns 1-10 for exact HTML structure)
  </read_first>
  <action>
**1. Add game card** (after the freecell button, before closing `</div>` of `.game-grid`, around line 496):

```html
<button class="game-btn" data-game="kruiswoord" onclick="Router.go('kruiswoord')">
  <span class="game-icon">✏️</span>
  <span class="game-name">Kruiswoord</span>
  <span class="game-sub">Raaisel</span>
  <span class="game-stats-chip" id="stats-kruiswoord"></span>
</button>
```

Add CSS for the 6th game button hover color. After the freecell hover rule (around line 139), add:
```css
.game-btn[data-game="kruiswoord"]:hover { background: rgba(100, 180, 220, 0.08); }
.game-btn[data-game="kruiswoord"] .game-icon { color: #64b4dc; text-shadow: 0 0 20px rgba(100,180,220,0.4); }
```

Add 6th child animation delay after `nth-child(5)` (around line 198):
```css
.game-btn:nth-child(6) { animation-delay: 480ms; }
```

**2. Update welcome screen grid** to accommodate 6 buttons. Find the `.game-grid` CSS (around line 70) which currently uses `grid-template-columns: repeat(5, 1fr)`. Change to `repeat(3, 1fr)` so buttons flow as 2 rows of 3. In the portrait media query that already handles `repeat(3, 1fr)`, update the `nth-child(4)` and `nth-child(5)` rules — add `nth-child(6)` with `grid-column: 3` in the portrait/narrow block. Or if simpler, just change to `repeat(3, 1fr)` in both breakpoints (3 columns in landscape gives 2 rows of 3, same in portrait).

**3. Add kruiswoord screen section** (after the freecell screen section, before script tags). Model on `screen-sudoku`. The section must contain:

```html
<section id="screen-kruiswoord" data-screen="kruiswoord" class="screen">
  <div class="kw-container">
    <!-- Header -->
    <header class="game-header">
      <button class="btn-back" onclick="KruiswoordUI.handleBack()">&#8592; Terug</button>
      <div class="kw-header-center">
        <h1 style="font-family:var(--font-display);font-size:1.3rem;margin:0;">Kruiswoordraaisel</h1>
      </div>
      <div style="display:flex;gap:8px;">
        <button class="btn-icon" id="kw-undo-btn" title="Ontdoen">&#8617;</button>
        <button class="btn-icon" id="kw-newgame-btn" title="Nuwe Spel">&#x1F504;</button>
      </div>
    </header>

    <!-- Game area -->
    <div class="kw-game-area">
      <div class="kw-grid" id="kw-grid">
        <!-- Cells generated by JS -->
      </div>

      <!-- Clue panels -->
      <div class="kw-clues">
        <div class="kw-clue-col">
          <h3 class="kw-clue-heading">Dwarsrigting</h3>
          <div class="kw-clue-list" id="kw-clues-across"></div>
        </div>
        <div class="kw-clue-col">
          <h3 class="kw-clue-heading">Afrigting</h3>
          <div class="kw-clue-list" id="kw-clues-down"></div>
        </div>
      </div>
    </div>

    <!-- Hidden input for S Pen / keyboard -->
    <input id="kw-hidden-input" type="text"
           autocomplete="off" autocorrect="off" autocapitalize="characters"
           style="position:fixed;top:-200px;left:-200px;opacity:0;width:1px;height:1px;" />
  </div>

  <!-- Difficulty modal (visible on entry) -->
  <div class="win-overlay" id="kw-difficulty-modal" style="display:flex;">
    <div class="win-card">
      <h2 style="font-size:1.8rem;">Kruiswoordraaisel</h2>
      <p>Kies moeilikheidsgraad</p>
      <div style="display:flex;flex-direction:column;gap:12px;">
        <button class="btn btn-secondary" id="kw-diff-maklik">Maklik (9&times;9)</button>
        <button class="btn btn-secondary" id="kw-diff-medium">Medium (13&times;13)</button>
        <button class="btn btn-secondary" id="kw-diff-moeilik">Moeilik (17&times;17)</button>
      </div>
    </div>
  </div>

  <!-- Win overlay -->
  <div class="win-overlay" id="kw-win-overlay">
    <div class="win-card">
      <h2>Geluk!</h2>
      <p id="kw-win-message"></p>
      <div style="display:flex;gap:12px;justify-content:center;">
        <button class="btn btn-primary" id="kw-win-newgame">Nuwe Spel</button>
        <button class="btn btn-secondary" id="kw-win-home">Tuis</button>
      </div>
    </div>
  </div>

  <!-- Quit confirmation -->
  <div class="win-overlay" id="kw-confirm-quit">
    <div class="win-card">
      <h2 style="font-size:1.5rem;">Wil jy opgee?</h2>
      <p>Vordering sal verlore gaan.</p>
      <div style="display:flex;gap:12px;justify-content:center;">
        <button class="btn btn-primary" id="kw-quit-yes">Ja</button>
        <button class="btn btn-secondary" id="kw-quit-no">Nee</button>
      </div>
    </div>
  </div>
</section>
```

**4. Add inline CSS** for kruiswoord (in the `<style>` block alongside other game CSS). Add these styles:

```css
/* Kruiswoord layout */
.kw-container { padding: 8px 16px; max-width: 1200px; margin: 0 auto; height: 100%; display: flex; flex-direction: column; }
.kw-game-area { flex: 1; display: flex; flex-direction: column; align-items: center; gap: 16px; overflow-y: auto; padding-bottom: 20px; }
.kw-grid { display: grid; gap: 1px; background: #555; border: 2px solid #555; margin: 0 auto; max-width: 90vw; max-height: 50vh; aspect-ratio: 1; }
.kw-cell { display: flex; align-items: center; justify-content: center; position: relative; font-family: var(--font-body); font-weight: 700; }
.kw-cell-black { background: var(--bg-base); }
.kw-cell-white { background: #f5f0e6; color: #1a1610; cursor: pointer; }
.kw-cell-white:active { background: #e8e0cc; }
.kw-cell-num { position: absolute; top: 1px; left: 2px; font-size: 0.55em; font-weight: 400; color: rgba(26,22,16,0.5); line-height: 1; }
.kw-cell-letter { font-size: 1em; text-transform: uppercase; }
.kw-cell-selected { background: var(--accent-gold) !important; color: #1a1610; }
.kw-cell-highlighted { background: rgba(212,162,58,0.3) !important; }
.kw-cell-correct { background: #4caf78 !important; color: #fff; transition: background 0.1s; }
.kw-cell-locked { background: rgba(76,175,120,0.15) !important; pointer-events: none; color: #2a5a3a; }

/* Clue panels */
.kw-clues { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; width: 100%; max-width: 800px; }
.kw-clue-heading { font-family: var(--font-display); font-size: 1rem; color: var(--accent-gold); margin: 0 0 8px; }
.kw-clue-list { display: flex; flex-direction: column; gap: 4px; max-height: 200px; overflow-y: auto; }
.kw-clue-item { font-family: var(--font-body); font-size: 0.85rem; color: var(--text-primary); padding: 4px 8px; border-radius: 4px; cursor: pointer; }
.kw-clue-item:hover { background: rgba(212,162,58,0.1); }
.kw-clue-item.kw-clue-active { background: rgba(212,162,58,0.25); color: var(--accent-gold); }
.kw-clue-item.kw-clue-done { text-decoration: line-through; opacity: 0.5; }
```

**5. Add script tags** (after the freecell ui.js script tag, around line 1060):
```html
<script src="games/kruiswoord/engine.js"></script>
<script src="games/kruiswoord/ui.js"></script>
```

**6. Update refreshStats** (around line 1076): Change the games array from:
```javascript
const games = ['woordsoek', 'solitaire', 'spider', 'sudoku', 'freecell'];
```
to:
```javascript
const games = ['woordsoek', 'solitaire', 'spider', 'sudoku', 'freecell', 'kruiswoord'];
```

**7. Update version span** (around line 504): Change `v1.0.4` to `v1.1.0`.

**8. Update manifest description** in the `<meta>` or anywhere the description mentions "Five" games — update to "Six" if found.
  </action>
  <verify>
    <automated>grep -c 'data-game="kruiswoord"' index.html | grep -q '^1$' && grep -c 'screen-kruiswoord' index.html | grep -q '^[1-9]' && grep -c 'games/kruiswoord/engine.js' index.html | grep -q '^[1-9]' && grep -q "'kruiswoord'" index.html && grep -q 'kw-grid' index.html && grep -q 'kw-difficulty-modal' index.html && grep -q 'kw-win-overlay' index.html && grep -q 'kw-confirm-quit' index.html && grep -q 'kw-hidden-input' index.html && grep -q 'v1.1.0' index.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains exactly one `data-game="kruiswoord"` button in the game-grid
    - index.html contains `id="screen-kruiswoord"` section with `data-screen="kruiswoord"`
    - index.html contains `id="kw-grid"` div
    - index.html contains `id="kw-clues-across"` and `id="kw-clues-down"` divs
    - index.html contains `id="kw-hidden-input"` input element
    - index.html contains `id="kw-difficulty-modal"` overlay with 3 difficulty buttons
    - index.html contains `id="kw-win-overlay"` overlay with `kw-win-newgame` and `kw-win-home` buttons
    - index.html contains `id="kw-confirm-quit"` overlay with `kw-quit-yes` and `kw-quit-no` buttons
    - index.html contains `<script src="games/kruiswoord/engine.js"></script>`
    - index.html contains `<script src="games/kruiswoord/ui.js"></script>`
    - refreshStats games array contains `'kruiswoord'`
    - index.html version span shows `v1.1.0`
    - CSS classes defined: `kw-cell`, `kw-cell-black`, `kw-cell-white`, `kw-cell-selected`, `kw-cell-highlighted`, `kw-cell-correct`, `kw-cell-locked`
    - `KruiswoordUI.handleBack()` is the onclick for the back button (not `Router.back()`)
  </acceptance_criteria>
  <done>
    Kruiswoord game card visible on welcome screen, full screen section HTML exists with grid, clue panels, hidden input, difficulty modal, win overlay, and quit confirmation. Script tags load engine.js and ui.js. Stats refresh includes kruiswoord. Version shows 1.1.0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add kruiswoord files to sw.js CORE_ASSETS and bump cache version</name>
  <files>sw.js, manifest.json</files>
  <read_first>
    - sw.js (CORE_ASSETS array around lines 11-40, CACHE_NAME on line 6)
    - manifest.json (version field on line 4)
  </read_first>
  <action>
**1. sw.js — Add kruiswoord files** to CORE_ASSETS array (after the freecell entries, before the closing `]`):
```javascript
  '/ConGames/games/kruiswoord/clues.json',
  '/ConGames/games/kruiswoord/engine.js',
  '/ConGames/games/kruiswoord/ui.js',
```

**2. sw.js — Bump CACHE_NAME** from `'congames-v1.0.4'` to `'congames-v1.1.0'`.

**3. manifest.json — Bump version** from `"1.0.4"` to `"1.1.0"`.

**4. manifest.json — Update description** from `"Five classic games"` to `"Six classic games"` and add Kruiswoord to the list: `"Six classic games for Con — Woord Soek, Solitaire, Spider, Sudoku, FreeCell, Kruiswoord"`.
  </action>
  <verify>
    <automated>grep -q 'kruiswoord/engine.js' sw.js && grep -q 'kruiswoord/ui.js' sw.js && grep -q 'kruiswoord/clues.json' sw.js && grep -q 'congames-v1.1.0' sw.js && grep -q '"1.1.0"' manifest.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - sw.js CORE_ASSETS contains `/ConGames/games/kruiswoord/clues.json`
    - sw.js CORE_ASSETS contains `/ConGames/games/kruiswoord/engine.js`
    - sw.js CORE_ASSETS contains `/ConGames/games/kruiswoord/ui.js`
    - sw.js CACHE_NAME is `'congames-v1.1.0'`
    - manifest.json `"version"` is `"1.1.0"`
    - manifest.json description mentions "Six" and "Kruiswoord"
  </acceptance_criteria>
  <done>
    Service worker caches all kruiswoord files. Cache version and manifest version both at 1.1.0.
  </done>
</task>

</tasks>

<verification>
- Open index.html in browser: kruiswoord game card appears as 6th button on welcome screen
- Click kruiswoord card: screen-kruiswoord section shows with difficulty modal visible
- No JS console errors on page load
- `grep -c kruiswoord sw.js` returns 3 (three file paths)
- `grep kruiswoord index.html | wc -l` returns multiple hits (game card, screen, scripts, stats)
</verification>

<success_criteria>
All kruiswoord DOM elements exist and are ready for ui.js to wire event handlers. Service worker will cache all game files. Version bumped to 1.1.0.
</success_criteria>

<output>
After completion, create `.planning/phases/12-ui-integration/12-01-SUMMARY.md`
</output>
